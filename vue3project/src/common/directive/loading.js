class Loading {
  constructor(el, binding, vnode) {
    this.el = el;
    this.binding = binding;
    this.vnode = vnode;
    this.init();
  }

  init() {
    let el = this.el;
    let binding = this.binding;
  }
}

export default Loading;
